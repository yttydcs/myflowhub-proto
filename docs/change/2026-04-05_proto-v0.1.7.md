# 2026-04-05 Proto：发布 graph contract closure v0.1.7

## 变更背景 / 目标
- 当前 `MyFlowHub-Proto` 主线已经收口了 flow graph/node canonical contract：
  - `protocol/flow/types.go` 成为唯一 wire truth
  - `docs/flow_contract.md` 与 `generated/flow_contract.ts` 已补齐
  - `docs/protocol_map.md` 与 `README.md` 已同步
- 但最新正式 tag 仍停在 `v0.1.6`，下游还无法通过 semver 拉取这一轮完整 contract。
- 本次目标是把当前已存在的 graph contract closure 作为正式 patch `v0.1.7` 发布，给 `SubProto/flow`、`Server` 和 `Win` 提供稳定上游。

## 具体变更内容
- 本次 workflow 不新增协议设计，只对当前主线已存在的 graph contract closure 做正式 patch release。
- 发布范围:
  - `protocol/flow/types.go`
  - `cmd/flowcontractgen/main.go`
  - `internal/flowcontract/*`
  - `docs/flow_contract.md`
  - `generated/flow_contract.ts`
  - `docs/protocol_map.md`
  - `README.md`
- 新发布 tag:
  - `v0.1.7`

## Requirements impact
`none`

## Specs impact
`none`

## Lessons impact
`none`

## Related requirements
- `none`

## Related specs
- `D:\project\MyFlowHub3\worktrees\proto-graph-contract-release-chain\docs\flow_contract.md`
- `D:\project\MyFlowHub3\worktrees\proto-graph-contract-release-chain\docs\protocol_map.md`

## Related lessons
- `D:\project\MyFlowHub3\docs\lessons\cross-repo-semver-release.md`
- `D:\project\MyFlowHub3\worktrees\proto-graph-contract-release-chain\docs\lessons\flow-graph-contract-canonical-sync.md`

## 对应 plan.md 任务映射
- `REL-1` - Proto release doc + commit + tag `v0.1.7`

## 经验 / 教训摘要
- graph contract 即使已经在 Proto 主线落地，只要 patch tag 没跟上，下游仍会在真实依赖解析中拿到旧 contract。
- 这类 patch release 的正确边界是“发布已有事实”，而不是在 release workflow 中继续改协议设计。

## 可复用排查线索
- 症状
  - 下游已经引用 `flow_contract.ts` / typed node spec / `edge.case`
  - 但 `go list -m github.com/yttydcs/myflowhub-proto` 仍落在 `v0.1.6`
- 触发条件
  - graph contract closure 已合入 Proto 主线，但未打新 tag
- 关键词
  - `flow graph contract`
  - `flow_contract.ts`
  - `NodeKind`
  - `BindingSourceKind`
  - `BranchMatchOp`
  - `edge.case`
- 快速检查
  - `git log v0.1.6..HEAD -- protocol/flow/types.go`
  - `GOWORK=off go test ./... -count=1 -p 1`

## 关键设计决策与权衡
- 采用 `v0.1.7` patch release，而不是让下游继续靠 `replace` 消费本地 worktree。
- 本次只发布已有 closure，不把新的 contract 设计混进 release workflow。

## 测试与验证方式 / 结果
- `GOWORK=off go test ./... -count=1 -p 1`
  - 结果：通过

## 潜在影响
- 下游如果继续锁定 `v0.1.6`，仍不会拿到本轮 graph contract closure。

## 回滚方案
- 若 tag 尚未推送：
  - 删除本地 tag
  - 回退本次 archive 提交
- 若 tag 已推送：
  - 不重写已发布 tag，改发更高 patch 版本

## 子Agent执行轨迹
- 本轮未使用子Agent。
